<%= form_with(model: retrospective) do |form| %>
  <% if retrospective.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(retrospective.errors.count, "error") %> prohibited this retrospective from being saved:</h2>

      <ul>
        <% retrospective.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :date_of_retro, style: "display: block" %>
    <%= form.datetime_field :date_of_retro %>
  </div>

  <div>
    <%= form.label :label, style: "display: block" %>
    <%= form.text_field :label %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
